// import * as ActionTypes from '../actions/ActionTypes';

// export const examples = (state = { errMess: null, examples:[]}, action) => {
//   switch (action.type) {
//     case ActionTypes.ADD_EXAMPLES:
//       return {...state, errMess: null, examples: action.payload};

//     case ActionTypes.EXAMPLES_FAILED:
//       return {...state, errMess: action.payload};

//     case ActionTypes.ADD_EXAMPLE:
//       var example = action.payload;
//       example.id = examples.length;
//       return {...state, examples: state.examples.concat(example)};

//     default:
//       return state;
//   }
// };
